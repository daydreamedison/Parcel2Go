<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wondermelonpapajoanne.joanne.parcel2go">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.customer.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".activity.driver.DriverMainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.customer.InputSenderInformationAndReceiverActivity"
            android:label="@string/title_activity_place_order_address"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.customer.PriceComparisonActivity"
            android:label="@string/title_activity_price_comparison"
            android:theme="@style/AppTheme" />

        <activity android:name=".activity.RegisterActivity"
            android:label="RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>